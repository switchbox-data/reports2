---
title: ANALYSIS TEMPLATE
toc: true
reference-location: margin
fig-cap-location: bottom
appendix-style: default
citation-location: document
citation:
  container-title: Switchbox
format:
  html:
    page-layout: full
---
```{r}
library(tidyverse)
library(tidyplots)
library(gt)
library(here)
here::i_am("reports/il_lea/notebooks/analysis.qmd")
source(here("lib", "ggplot", "switchbox_theme.R"))
```

```{r}
lea_df <- read_csv(here("reports", "il_lea", "data_clean", "il_lea_data.csv"))
```

```{r}
pipe_colors <- c(
  "main" = sb_colors["carrot"][[1]],
  "service" = sb_colors["midnight"][[1]],
  "combined" = sb_colors["sky"][[1]]
)
```

```{r}
# Function to filter dataframe based on filter string
# Example: filter_spending(total_spending, "nicor_main_historical")
fetch_spending_vars <- function(target, df, filter_str) {
  # Split the filter string by underscores
  parts <- str_split(filter_str, "_")[[1]]

  # Extract the three parts
  utility_val <- parts[1]
  main_service_val <- parts[2]
  historical_val <- parts[3]

  # Filter the dataframe
  var <- df |>
    filter(
      utility == utility_val,
      main_service == main_service_val,
      historical == historical_val
    ) |>
    pull(target)

  if (target %in% c("avg_cost", "total")) {
    return(scales::label_dollar(suffix = "M", prefix = "$", scale = 1e-6, accuracy = 1)(var))
  } else {
    return(var)
  }
}

spending_plot <- function(df, max_lim, breaks_step) {
df |>
  mutate(alpha_val = if_else(historical == "forecast", 0.7, 1)) |>
  ggplot(aes(
    x = factor(year),
    y = lea_cost,
    fill = main_service,
    alpha = alpha_val
  )) +
  geom_col(position = "stack") +
  scale_alpha_identity() +
  scale_y_continuous(limits = c(0, max_lim), breaks = seq(0, max_lim, by = breaks_step), labels = function(x) {
    scales::label_dollar(suffix = " million", prefix = "$", scale = 1e-6)(x)
  }) +
  scale_fill_manual(values = pipe_colors, drop = TRUE) +
  labs(
    x = "Year",
    y = "LEA Subsidy ($ millions)",
    fill = "Pipeline Type",
    caption = "Note: Forecast data shown with reduced opacity"
  )
}

per_connection_plot <- function(df, max_lim, breaks_step) {
  df |>
    mutate(alpha_val = if_else(historical == "forecast", 0.7, 1)) |>
    ggplot(aes(x = factor(year), y = cost_per_connection, fill = main_service, alpha = alpha_val)) +
    geom_col(position = "stack") +
    scale_alpha_identity() +
    scale_y_continuous(limits = c(0, max_lim), breaks = seq(0, max_lim, by = breaks_step), labels = function(x) {
      scales::label_dollar(suffix = "k", prefix = "$", scale = 1e-3)(x)
    }) +
    scale_fill_manual(values = pipe_colors, drop = TRUE) +
    labs(
      x = "Year",
      y = "LEA cost per new connection ($ thousands)",
      fill = "Pipeline Type",
      # caption = "Note: Forecast data shown with reduced opacity"
    )

}
# Example usage:
# fetch_spending_vars("avg_cost", total_spending, "nicor_main_historical")
# filter_spending("from", total_spending, "peoples_service_forecast")
# filter_spending("to", total_spending, "ameren_combined_historical")
```

```{r}
#| label: fig-nicor-lea-trend
#| fig-cap: "Historical and forecasted LEA costs for Nicor Gas. Forecast data shown with reduced opacity."
#| fig-width: 7
lea_df |>
  filter(utility == "nicor") |>
  spending_plot(max_lim = 70e6, breaks_step = 10e6) +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

```{r}
#| label: fig-peoples-lea-trend
#| fig-cap: "Historical and forecasted LEA costs for Peoples Gas disclosed in 2023. Forecast data shown with reduced opacity."
lea_df |>
  filter(utility == "peoples") |>
  filter(!is.na(lea_cost)) |>
  spending_plot(max_lim = 30e6, breaks_step = 5e6)
```

```{r}
#| label: fig-ameren-spending-trend
#| fig-cap: "Historical and forecasted spending on new service requests for Ameren Gas (SWO C3231 proxy). Forecast data shown in lighter shading."
lea_df |>
  filter(utility == "ameren") |>
  filter(!is.na(lea_cost)) |>
  spending_plot(max_lim = 25e6, breaks_step = 5e6) +
  labs(y = "Spending on new service requests ($ millions)")
```

```{r}
#| label: fig-nicor-per-connection-cost
#| fig-cap: 'Historical LEA costs per connection for Nicor Gas. Service line estimates are based on the average reported "per project" cost, which are interpreted to mean connections. Costs may reflect both residential and non-residential connections. Forecast data shown with reduced opacity.'

lea_df |>
  filter(utility == "nicor") |>
  mutate(cost_per_connection = if_else(is.na(cost_per_connection) & main_service == "service", cost_per_project, cost_per_connection)) |>
  filter(!is.na(cost_per_connection), year <=2023, year >= 2019) |>
  per_connection_plot(max_lim = 20e3, breaks_step = 2e3)
```

NOTE: This is the only figure for Peoples Gas that is specific to single-family homes. The rest are not specified.

```{r}
#| label: fig-peoples-per-connection-cost
#| fig-cap: "Historical LEA costs per connection for new single-family residential connections to Peoples Gas. Combined main extension and service line cost."


lea_df |>
  filter(utility == "peoples") |>
  filter(!is.na(cost_per_connection)) |>
  per_connection_plot(max_lim = 14e3, breaks_step = 2e3)
```

```{r}
#| label: fig-ameren-per-sce
#| fig-cap: "Historical standard cost equivalent per connection for Ameren Gas. Covers main extensions only; service line costs are not reported separately."
lea_df |>
  filter(utility == "ameren") |>
  filter(!is.na(max_lea_cost_per_connect)) |>
  mutate(alpha_val = if_else(historical == "forecast", 0.7, 1)) |>
  ggplot(aes(
    x = factor(year),
    y = max_lea_cost_per_connect,
    fill = main_service,
    alpha = alpha_val
  )) +
  geom_col(position = "stack") +
  scale_alpha_identity() +
  scale_y_continuous(
    limits = c(0, 8e3),
    breaks = seq(0, 8e3, by = 2e3),
    labels = function(x) {
      scales::label_dollar(suffix = "k", prefix = "$", scale = 1e-3)(x)
    }
  ) +
  scale_fill_manual(values = pipe_colors, drop = TRUE) +
  labs(
    x = "Year",
    y = "Maximum LEA cost per new connection ($ thousands)",
    fill = "Pipeline Type",
    # caption = "Note: Forecast data shown with reduced opacity"
  )
```

```{r}
#| label: fig-statewide-lea-2022
#| fig-cap: "Total 2022 LEA spending by utility, split by pipeline type. Nicor accounts for more than half of statewide LEA spending."
#| fig-width: 7

lea_df |>
  filter(year == 2022, !is.na(lea_cost)) |>
  mutate(utility = str_to_title(utility)) |>
  mutate(utility = fct_reorder(utility, lea_cost, sum, .desc = TRUE)) |>
  ggplot(aes(x = utility, y = lea_cost, fill = main_service)) +
  geom_col(position = "stack") +
  scale_y_continuous(labels = scales::label_dollar(suffix = " million", scale = 1e-6)) +
  scale_fill_manual(values = pipe_colors) +
  labs(x = "Utility", y = "2022 LEA Subsidy ($ millions)", fill = "Pipeline Type")
```

```{r}
#| label: fig-lifecycle-cost
#| fig-cap: "One year (2022) of LEA spending and its full lifecycle cost to ratepayers, by utility. Labels show each utility's lifecycle multiplier: total ratepayer cost per dollar of LEA spending. Nominal dollars over each asset's approved depreciation life. Source: Switchbox amortization model built from approved parameters in ICC Dockets 25-0055 (Nicor), 23-0068/69 Cons. (Peoples Gas), and 25-0084 (Ameren)."
#| fig-width: 7

# Lifecycle costs and multipliers come from the Switchbox amortization model
# (see the report's Data and Methods appendix), not from the rate-case data in
# lea_df: 2022 net LEA capex amortized over each asset's approved depreciation
# life at the approved pre-tax rate of return, with IRS tax normalization.
lifecycle_df <- tribble(
  ~utility, ~measure, ~cost,
  "Nicor Gas", "2022 LEA spending", 65.7e6,
  "Nicor Gas", "Lifecycle cost to ratepayers", 181.9e6,
  "Ameren", "2022 LEA spending", 21.3e6,
  "Ameren", "Lifecycle cost to ratepayers", 51.2e6,
  "Peoples Gas", "2022 LEA spending", 26.7e6,
  "Peoples Gas", "Lifecycle cost to ratepayers", 50.9e6
) |>
  mutate(
    utility = fct(utility, levels = c("Nicor Gas", "Ameren", "Peoples Gas")),
    measure = fct(measure, levels = c("2022 LEA spending", "Lifecycle cost to ratepayers"))
  )

multiplier_labels <- tribble(
  ~utility, ~cost, ~label,
  "Nicor Gas", 181.9e6, "2.8x",
  "Ameren", 51.2e6, "2.4x",
  "Peoples Gas", 50.9e6, "1.9x"
) |>
  mutate(utility = fct(utility, levels = c("Nicor Gas", "Ameren", "Peoples Gas")))

ggplot(lifecycle_df, aes(x = utility, y = cost, fill = measure)) +
  geom_col(position = position_dodge(width = 0.9), width = 0.8) +
  geom_text(
    data = multiplier_labels,
    aes(x = utility, y = cost, label = label),
    inherit.aes = FALSE,
    nudge_x = 0.225, vjust = -0.6, size = 11 / .pt, family = "IBM Plex Sans"
  ) +
  scale_y_continuous(
    labels = scales::label_dollar(suffix = " million", scale = 1e-6),
    expand = expansion(mult = c(0, 0.08))
  ) +
  scale_fill_manual(values = c(
    "2022 LEA spending" = sb_colors["midnight"][[1]],
    "Lifecycle cost to ratepayers" = sb_colors["carrot"][[1]]
  )) +
  labs(x = "Utility", y = "2022 cohort cost ($ millions, nominal)", fill = NULL)
```

```{r}
#| label: vars

total_spending <- lea_df |>
  filter(!is.na(lea_cost)) |>
  summarize(
    .by = c(utility, main_service, historical),
    avg_cost = mean(lea_cost),
    total = sum(lea_cost),
    from = min(year),
    to = max(year)
  )

lea_total_all_utilities_22 <- lea_df |>
  filter(!is.na(lea_cost)) |>
  filter(year == 2022) |>
  summarize(
    .by = c(utility, main_service, historical, year),
    total = sum(lea_cost),
  ) |>
  pull(total) |>
  sum() |>
  (\(x) scales::label_dollar(suffix = "M", prefix = "$", scale = 1e-6, accuracy = 0.1)(x))()

lea_hist_avg_all_utilities_18to22 <- lea_df |>
  filter(!is.na(lea_cost), historical == "historical") |>
  summarize(.by = year,
    total = sum(lea_cost),
  )|>
  pull(total) |>
  mean() |>
  (\(x) scales::label_dollar(suffix = "M", prefix = "$", scale = 1e-6, accuracy = 0.1)(x))()

nicor_per_connect_23 <- lea_df |>
  filter(!is.na(cost_per_connection)) |>
  filter(utility == "nicor", year == max(year)) |>
  pull(cost_per_connection) |>
  (\(x) scales::label_dollar(prefix = "$")(x))()

nicor_per_connect_service_23 <- lea_df |>
  filter(!is.na(cost_per_project)) |>
  filter(utility == "nicor", year == max(year), main_service == "service") |>
  pull(cost_per_project) |>
  (\(x) scales::label_dollar(prefix = "$", accuracy = 1)(x))()

nicor_per_connect_change <- lea_df |>
  filter(!is.na(cost_per_connection)) |>
  filter(utility == "nicor") |>
  arrange(main_service, year) |>
  group_by(main_service) |>
  mutate(
    yoy_change = (cost_per_connection - lag(cost_per_connection)) /
      lag(cost_per_connection)
  ) |>
  summarize(avg_annual_increase = mean(yoy_change, na.rm = TRUE)) |>
  pull(avg_annual_increase) |>
  (\(x) scales::percent(x))()


peoples_per_connect_22 <- lea_df |>
  filter(utility == "peoples", !is.na(cost_per_connection)) |>
  filter(year == max(year)) |>
  pull(cost_per_connection) |>
  (\(x) scales::label_dollar(prefix = "$")(x))()

```

```{r}
#| label: tbl-data-availability
#| tbl-cap: "Data availability by utility and metric. Green indicates data provided directly by utility, yellow indicates estimated/calculated values, red indicates missing data."

# Create data availability matrix - color info embedded in data
data_avail <- tribble(
  ~Utility, ~Period, ~`Mains Total`, ~`Services Total`, ~`Mains Per Connection`, ~`Mains Per Customer`, ~`Services Per Connection`, ~`Services Per Customer`, ~`N Mains`, ~`N Services`,
  # Nicor Historical
  "Nicor Gas", "Historical",
  "2018-2022|green", "2018-2022|green", "red", "2019-2023|green", "2018-2022|yellow", "red", "2018-2022|green", "2018-2022|green",
  # Nicor Forecast
  "Nicor Gas", "Forecast",
  "2023-2030|green", "2023-2030|green", "red", "red", "2023-2026|yellow", "red", "2023-2026|green", "2023-2026|green",
  # Ameren Historical
  "Ameren Gas", "Historical",
  "2021-2022|yellow", "2021-2022|yellow", "2021-2025|yellow", "red", "red", "red", "red", "red",
  # Ameren Forecast
  "Ameren Gas", "Forecast",
  "2023-2024|yellow", "2023-2025|yellow", "red", "red", "red", "red", "red", "red",
  # Peoples Historical
  "Peoples Gas", "Historical",
  "2018-2022|yellow", "2018-2022|yellow", "red", "red", "2020-2022;2023|yellow", "2020-2022|yellow", "2018-2022|yellow", "2018-2022|green",
  # Peoples Forecast
  "Peoples Gas", "Forecast",
  "2023-2024|yellow", "red", "red", "red", "red", "red", "2023-2024|yellow", "red"
)

# Extract display text
data_display <- data_avail |>
  mutate(across(-c(Utility, Period), ~ {
    case_when(
      str_detect(.x, "\\|green") ~ str_remove(.x, "\\|green"),
      str_detect(.x, "\\|yellow") ~ str_remove(.x, "\\|yellow"),
      .x == "red" ~ "—",
      TRUE ~ .x
    )
  }))

# Extract colors
data_colors <- data_avail |>
  mutate(across(-c(Utility, Period), ~ {
    case_when(
      str_detect(.x, "\\|green") ~ "green",
      str_detect(.x, "\\|yellow") ~ "yellow",
      .x == "red" ~ "red",
      TRUE ~ "none"
    )
  }))

# Color mapping
color_hex <- c(green = "#d4edda", yellow = "#fff3cd", red = "#f8d7da", none = "#ffffff")

# Create base gt table
gt_table <- data_display |>
  gt() |>
  cols_label(
    Period = "Period",
    `Mains Total` = "Mains Total",
    `Services Total` = "Services Total",
    `Mains Per Connection` = "Per Connection",
    `Mains Per Customer` = "Per Customer",
    `Services Per Connection` = "Per Connection",
    `Services Per Customer` = "Per Customer",
    `N Mains` = "Mains",
    `N Services` = "Services"
  ) |>
  tab_spanner(
    label = "Total Spending Allowance",
    columns = c(`Mains Total`, `Services Total`)
  ) |>
  tab_spanner(
    label = "Mains Allowance Spending",
    columns = c(`Mains Per Connection`, `Mains Per Customer`)
  ) |>
  tab_spanner(
    label = "Services Allowance Spending",
    columns = c(`Services Per Connection`, `Services Per Customer`)
  ) |>
  tab_spanner(
    label = "Projects Per Year",
    columns = c(`N Mains`, `N Services`)
  ) |>
  cols_align(
    align = "center",
    columns = -c(Utility, Period)
  ) |>
  tab_options(
    table.font.size = px(11),
    column_labels.font.weight = "bold"
  ) |>
  tab_footnote(
    footnote = "Green: Provided directly by utility | Yellow: Best estimate or calculated | Red: Not available"
  ) |>
  tab_source_note(
    source_note = "Nicor 2027–2030 figures are combined-only (from ICC Docket 25-0055); service/main breakdown is available only through 2026 (from ICC Docket 23-0066)."
  )

# Apply colors programmatically from the data
data_cols <- names(data_display)[-(1:2)]  # All columns except Utility and Period

for (i in 1:nrow(data_display)) {
  for (col_name in data_cols) {
    cell_color <- data_colors[[col_name]][i]
    if (cell_color != "none") {
      gt_table <- gt_table |>
        tab_style(
          style = cell_fill(color = color_hex[cell_color]),
          locations = cells_body(columns = all_of(col_name), rows = i)
        )
    }
  }
}

gt_table
```

```{r}
save.image(here("reports", "il_lea", "report_vars.RData"))
```
